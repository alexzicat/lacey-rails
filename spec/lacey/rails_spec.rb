require 'spec_helper'

describe Lacey::Rails do
  it 'has a version number' do
    expect(Lacey::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
